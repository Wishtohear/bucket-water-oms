# 水厂订货管理系统 - 环境变量配置示例

## 📋 配置文件说明

系统使用 `application.yml` 作为主配置文件，支持通过环境变量覆盖默认配置。

### 开发环境配置 (application-dev.yml)

已在 `application.yml` 中配置，使用默认的开发环境参数。

### 生产环境配置

生产环境建议通过环境变量或 `application-prod.yml` 进行配置。

## 🔧 数据库配置

| 环境变量 | 说明 | 默认值 |
|---------|------|--------|
| `DB_HOST` | PostgreSQL 主机地址 | `localhost` |
| `DB_PORT` | PostgreSQL 端口 | `5432` |
| `DB_NAME` | 数据库名称 | `bucket_water_oms` |
| `DB_USERNAME` | 数据库用户名 | `postgres` |
| `DB_PASSWORD` | 数据库密码 | `postgres` |

### Druid 连接池配置

| 环境变量 | 说明 | 默认值 |
|---------|------|--------|
| `DRUID_INITIAL_SIZE` | 初始连接数 | `5` |
| `DRUID_MIN_IDLE` | 最小空闲连接数 | `5` |
| `DRUID_MAX_ACTIVE` | 最大活跃连接数 | `20` |
| `DRUID_MAX_WAIT` | 最大等待时间(ms) | `60000` |

## 🔴 Redis 配置

| 环境变量 | 说明 | 默认值 |
|---------|------|--------|
| `REDIS_HOST` | Redis 主机地址 | `localhost` |
| `REDIS_PORT` | Redis 端口 | `6379` |
| `REDIS_DB` | Redis 数据库编号 | `0` |
| `REDIS_PASSWORD` | Redis 密码 | (空) |
| `REDIS_MAX_ACTIVE` | 最大连接数 | `8` |
| `REDIS_MAX_IDLE` | 最大空闲连接数 | `8` |
| `REDIS_MIN_IDLE` | 最小空闲连接数 | `0` |

## 🔐 JWT 配置

| 环境变量 | 说明 | 默认值 |
|---------|------|--------|
| `JWT_SECRET` | JWT 密钥 | `your-secret-key-change-in-production` |
| `JWT_EXPIRATION` | Token 过期时间(ms) | `86400000` (24小时) |
| `JWT_HEADER` | 请求头名称 | `Authorization` |
| `JWT_PREFIX` | Token 前缀 | `Bearer` |

## 💻 Windows 环境变量设置

### 临时设置（当前终端会话）

```powershell
$env:DB_HOST="localhost"
$env:DB_PORT="5432"
$env:DB_NAME="bucket_water_oms"
$env:DB_USERNAME="postgres"
$env:DB_PASSWORD="your_password"

$env:REDIS_HOST="localhost"
$env:REDIS_PORT="6379"
$env:REDIS_PASSWORD="your_redis_password"

$env:JWT_SECRET="your-production-secret-key-must-be-long-and-secure"
```

### 永久设置（系统环境变量）

#### 方法1：使用 PowerShell

```powershell
[System.Environment]::SetEnvironmentVariable("DB_HOST", "localhost", [System.EnvironmentVariableTarget]::User)
[System.Environment]::SetEnvironmentVariable("DB_PASSWORD", "your_password", [System.EnvironmentVariableTarget]::User)
```

#### 方法2：使用 setx 命令

```cmd
setx DB_HOST "localhost"
setx DB_PORT "5432"
setx DB_PASSWORD "your_password"
```

#### 方法3：通过系统属性设置

1. 右键点击"此电脑" → "属性"
2. 点击"高级系统设置"
3. 点击"环境变量"按钮
4. 在"用户变量"或"系统变量"中添加上述环境变量

## 🐧 Linux/Mac 环境变量设置

### 临时设置（当前终端会话）

```bash
export DB_HOST="localhost"
export DB_PORT="5432"
export DB_NAME="bucket_water_oms"
export DB_USERNAME="postgres"
export DB_PASSWORD="your_password"

export REDIS_HOST="localhost"
export REDIS_PORT="6379"
export REDIS_PASSWORD="your_redis_password"

export JWT_SECRET="your-production-secret-key-must-be-long-and-secure"
```

### 永久设置

#### Linux (Ubuntu/Debian)

```bash
# 编辑 ~/.bashrc 或 ~/.profile
nano ~/.bashrc

# 添加以下内容
export DB_HOST="localhost"
export DB_PASSWORD="your_password"
# ... 其他变量

# 使配置生效
source ~/.bashrc
```

#### Mac

```bash
# 编辑 ~/.zshrc 或 ~/.bash_profile
nano ~/.zshrc

# 添加环境变量配置
export DB_HOST="localhost"
export DB_PASSWORD="your_password"
# ... 其他变量

# 使配置生效
source ~/.zshrc
```

## 🐳 Docker 环境变量设置

### docker run

```bash
docker run -d \
  -e DB_HOST=postgres \
  -e DB_PASSWORD=your_password \
  -e REDIS_HOST=redis \
  -e JWT_SECRET=your-production-secret \
  -p 8080:8080 \
  bucket-water-oms
```

### docker-compose.yml

```yaml
version: '3.8'
services:
  app:
    image: bucket-water-oms
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=bucket_water_oms
      - DB_USERNAME=postgres
      - DB_PASSWORD=your_password
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=your_redis_password
      - JWT_SECRET=your-production-secret
    ports:
      - "8080:8080"
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:16
    environment:
      - POSTGRES_DB=bucket_water_oms
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=your_password
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass your_redis_password
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
```

## ☁️ 云服务环境变量设置

### 阿里云

在阿里云 ECS 或容器服务中，通过"实例详情" → "本实例环境变量"进行设置。

### 腾讯云

在腾讯云 CVM 或容器服务中，通过"环境配置"进行设置。

### AWS

```bash
# 使用 AWS CLI 设置环境变量
aws ssm put-parameter --name "/bucket-water-oms/DB_PASSWORD" --value "your_password" --type SecureString
aws ecs update-service --cluster your-cluster --service your-service --environment 'name=DB_PASSWORD,value=your_password'
```

## 🔒 安全建议

### 1. 生产环境必须修改的变量

- [ ] `DB_PASSWORD` - 数据库密码（必须设置为强密码）
- [ ] `REDIS_PASSWORD` - Redis 密码（建议设置）
- [ ] `JWT_SECRET` - JWT 密钥（必须设置为长且安全的随机字符串）

### 2. 生成强密码的方法

#### PowerShell

```powershell
# 生成随机密码
$bytes = New-Object byte[] 32
$rng = [System.Security.Cryptography.RandomNumberGenerator]::Create()
$rng.GetBytes($bytes)
[Convert]::ToBase64String($bytes)
```

#### Linux/Mac

```bash
# 使用 openssl 生成随机密钥
openssl rand -base64 32

# 使用 /dev/urandom 生成随机密码
head -c 32 /dev/urandom | base64
```

### 3. 使用密钥管理服务

生产环境建议使用密钥管理服务（如阿里云 KMS、AWS Secrets Manager）来管理敏感配置。

## 📝 启动应用

配置完环境变量后，启动应用：

```bash
mvn spring-boot:run
```

或使用打包后的 JAR 文件：

```bash
java -jar target/oms-1.0.0-SNAPSHOT.jar
```

## ✅ 验证配置

应用启动后，可以通过以下方式验证配置：

1. **检查启动日志**：确认数据库和 Redis 连接成功
2. **访问 Swagger UI**：http://localhost:8080/swagger-ui.html
3. **健康检查**：http://localhost:8080/actuator/health
